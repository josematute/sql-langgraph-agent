# Copy this file to .env and fill in your values

# OpenAI Configuration (required for default)
OPENAI_API_KEY=your_openai_api_key_here

# AWS Bedrock Configuration (optional - use profile OR credentials, not both)
# Option 1: Use AWS Profile (recommended - reads from ~/.aws/credentials)
# AWS_PROFILE=your_aws_profile_name
# AWS_REGION=us-east-1

# Option 2: Use Direct Credentials
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1

# Model Provider Selection (optional)
# MODEL_PROVIDER=bedrock
# MODEL_PROVIDER=openai
# BEDROCK_MODEL_ID=anthropic.claude-haiku-4-5-20251001-v1:0

# Database Connection (optional)
# Supports any SQL database: PostgreSQL, SQLite, MySQL, etc.
# DATABASE_URI=postgresql://user:password@host:port/database
# DATABASE_URI=sqlite:///path/to/database.db
# DATABASE_URI=mysql://user:password@host:port/database

# LangSmith Tracing (optional)
# LANGSMITH_API_KEY=your_langsmith_api_key_here
# LANGSMITH_TRACING=true
# LANGSMITH_PROJECT=sql-langgraph-agent
